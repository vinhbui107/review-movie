import axiosClient from "./axiosClient";

const userApi = {
    login: () => {
        // const url = "/products";
        // return axiosClient.get(url, { params });
    },

    register: () => {},

    confirmRegister: () => {},
};

export default userApi;
